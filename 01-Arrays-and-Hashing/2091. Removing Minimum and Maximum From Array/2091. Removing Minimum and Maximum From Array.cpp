            if (nums[i] < mini.first) {                mini = {nums[i], i};            }        }        int n = nums.size();        int left_idx = min(mini.second, maxi.second);        int right_idx = max(mini.second, maxi.second);        // 1. Delete both from front: right_idx + 1        // 2. Delete both from back:  n - left_idx            }                maxi = {nums[i], i};            if (nums[i] > maxi.first) {        // 3. Delete from both ends:  (left_idx + 1) + (n - right_idx)        return min({right_idx + 1, n - left_idx, (left_idx + 1) + (n - right_idx)});    }};// { a b c i d f g i}// { a i a a a a a a a i}